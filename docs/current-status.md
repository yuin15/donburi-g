# Slot-chanの現在地

2026-09-12。無料の60秒CPUスロット対戦が公開済み。**PC専用、横画面1280×720以上、マウス・キーボード**を対象にする。音声・映像は任意で、APIキー、招待、マイク、Upstashなしに遊べる。

## ゲームと画面

- #72でMVVMへ分離。Modelは既存のゲーム規則、ViewModelはCPU/Live進行・予約・タイマー・表示状態、ViewはDOM・入力・Three.jsを担当する。mainは40行の起動処理、DEV検収は専用モジュールに整理した。[構成と変更箇所](architecture.md)。
- #64で手動回転に変更。クリック・Spaceで両者1回転、回転中の連打は次の1回だけ予約。無入力では回らず、60秒以内に実際に受け付けた回転数で競う。間隔1.1秒以上、最大55回、結果に実回転数を表示する。
- 中央1ライン、配当120/240/1200、20秒・40秒に2択改造、勝利・敗北・引き分け、再戦まで実装。選択は24秒・44秒の期限で確定し、遅れた入力や二重入力で延長しない。改造パネルを右へ移し、選択中も回すボタンを操作できる。
- #46で改造5秒前のプレビュー、現在→改造後の当たり率/7揃い率、未選択時の適用通知を追加。公開Chromeの60秒対戦で確認済み。
- #50/#51で両者の出目に合うCPU台詞と表情、結果の配当内訳・改造順・最高の一回を追加。確定集計をsnapshotに含め、描画の欠落から回復しても全回転の集計を保持する。
- #63で改造構成と同じ枚数の絵柄を表示し、相手の小リールも実際に下向きに回転させた。両者の構成バーに絵柄数を表示する。
- #68で相手の当たりに青い小リール発光・加点・効果音を追加。連打中も両加点と自分の金貨を短い表示寿命まで残す。相手の改造中・確定後の追加絵柄を、台詞に消されない構成バー上で知らせる。
- #70で改造開始時の自動フォーカスを削除。Space連打が安定型の誤選択へ切り替わらず、クリック・1/2キー・Tabで選べる。
- #75で開始カウントダウンへフォーカスを置き、先行するSpaceで誤退出する問題を修正。操作キーを添えた開始表示へ整えた。[開始操作の確認](countdown-start.md)。
- #60で勝敗別の色と大きなコイン表示、点差、開閉する内訳、勝利の金貨演出を追加。改造カードは色・大きいキー・残り時間バー・選択済みマークで見分けられる。
- #52でLive字幕を定型文による上書きから保護し、再戦準備の得点・時間・内訳を初期化。実APIを使用しないDEV表示順の検収を記録した。
- #58で試合終了時にマイクを停止し、旧音声・旧字幕を接続世代で遮断。結果専用GPTは旧接続のcloseとLiveAvatarの音声消去確認後に1回だけ開始する。失敗しても確定結果を残す。
- 参考画像を基に筐体・操作台・専用絵柄・架空の成人ライバル4表情を制作。1つのThree.js描画へ集約し、HTMLで文字・操作を重ねる。
- リールは同じ絵柄が上から下へ連続して流れ、左→中→右に停止。両得点・台詞・結果は確定した停止フレームの後に更新する。相手の小リールは正方形の絵柄を保ち、縦潰れを修正した。
- #43でスマホ用の縦配置・縮小UI・背景切り出しを削除。狭いウィンドウは最小幅1280pxと横スクロールでPC構図を維持する。スマホ用UI・タッチ最適化は対象外。
- 静止/非表示では連続描画せず、素材4点・概算20.4MiB。任意LiveClient/LiveKitはCPU入口で読み込まない。MVVM版の主JSは141.06KB gzip、CSSは5.48KB gzip。新しいフレームワークは追加していない。

## 確認した範囲

- 型検査、lint、210テスト、生成済みNode ESMの起動と拒否応答、本番ビルドが成功。実localhost WebSocket＋MatchSession結合2ケースは手動spin要求で確認した。認証・利用枠・外部プロバイダーは代替。500KB超のビルド警告は残る。
- MVVMの時計・予約・停止同期・接続世代・字幕保護をDOMなしの6境界テストで確認。ローカルChromeの勝利・敗北・改造2回・再戦、結果詳細の開閉保持を確認した。[MVVM移行確認](mvvm-verification.md)。
- 公開Chromeの手動対戦は32回転で840対1,080。無操作0回、連打予約、改造、60秒の最終停止待ち、結果、再戦後0回への初期化を確認した。旧自動版では25秒のページ停止から30回転の結果へ復旧、カウントダウン取消、効果音ミュートも確認済み。
- PCの1280×720 / 1920×1080を目視。通常・小当たり・7揃い・逆転・同点・最終スピンはDEV専用の検収画面でも確認し、回転動画と連続フレームを記録した。固定出目は本番に含まれない。
- 改造リールの録画時は406標本、中央値16.7ms、p95 16.8ms、最大33描画・252三角形。高性能PC上の測定であり、全端末の性能保証ではない。
- 公開CPU入口の通信は外部/API要求なし、初期約0.684MB（PR #41観測）。任意Live無効時のHTTP 401 / WebSocket拒否1008を、PR #42の公開版でも確認した。
- 個人メール・実会話を記録しない。秘密値はGit対象外のローカル環境とVercelのサーバーSecretsで管理し、追跡ファイル・ビルド・コミット差分を検査する。

詳細: [相手の当たり・連打中の加点](rival-feedback.md)、[手動回転の操作・公開確認](manual-spin.md)、[改造リールの表示](visible-upgrades.md)、[勝敗・改造の見栄え改善](duel-polish-verification.md)、[通信・期限の検証](live-sync-verification.md)、[ゲーム規則](game-rules.md)。[バランス測定](game-balance.md)は旧自動30回転の測定、[旧完了監査](completion-audit.md)はPR #34時点の履歴。

## 公開記録

- URL: https://slot-chan.vercel.app
- 公開コード: `2e749af13e13c526f7cb2dfcaef75b4fbdbf4350`（PR #73）。手動回転、両者の改造リール、結果音声分離も含むMVVM版。
- Vercel READY: `dpl_Ft8atN2hnqA8ChSJY2mgJtuoTPjN`
- マージ後CI: https://github.com/yuin15/donburi-g/actions/runs/34684527781 （success）
- #63/#64/#68/#70/#72をクローズ。主JS `index-DR4oln0M.js`、CSS `index-BgFTt2wz.css`。公開Chromeで37回転、1,200対2,160の結果・内訳・最終停止・再戦初期化を確認。外部/API通信0、JavaScriptエラー0。[MVVMの公開確認](mvvm-verification.md)。
- PR #71の公開ChromeでもSpace連打中の改造は未選択で、2キーで大勝負を選べた。[改造フォーカスの修正](upgrade-spin-focus.md)。
- PR #69の公開Chromeでは35回転、1,440対1,920。相手の加点保持・改造通知・再戦時の初期化、外部/API通信0・JavaScriptエラー0を確認。[公開記録](rival-feedback.md)。この操作で発見した改造フォーカスの問題はPR #71で修正した。
- 公開Chromeで32回転、840対1,080の敗北。残り0秒で31回転目の表示を保持して最終停止を待ち、32回転目の確定後に結果を表示した。再戦で0点・0回・基本構成に戻り、Space＋予約で2回転。外部/API通信0、JavaScriptエラー0、公開HTTP 401 / WebSocket1008を確認。[手動回転の公開記録](manual-spin.md)。
- PR #61の公開Chromeでは安定型→大勝負をキー1→2で選び、60秒で480対2,880。配当内訳・点差・改造順が一致、内訳の開閉で再戦ボタンが隠れず、再戦で0点・60秒・内訳0行・閉じた状態に戻る。外部/API通信0、JavaScriptエラー0。公開HTTP 401 / WebSocket1008も確認。詳しくは [勝敗・改造の検証](duel-polish-verification.md)。
- 公開Chromeで大勝負→安定型を数字キー2→1で選び、60秒で360対720の敗北。両者の配当内訳・合計点・改造順が一致した。再戦は0対0・60秒・未改造・内訳0行、Enter取消も確認。外部/API通信0、JavaScriptエラー0。認証なしHTTP 401 / WebSocket拒否1008も再確認。
- PR #56では画面のJS/CSSハッシュが変わらないことを確認し、公開入口と結果表、JSエラー0、HTTP 401 / WebSocket1008を再確認した。実Liveの有料接続は行っていない。
- Git連動の自動デプロイは未接続。接続済みVercel APIで公開する。

## 今回スキップする確認

ユーザーの「できない項目はスキップして別の作業を進める」方針に従う。以下を完了扱いで閉じず、無料CPU対戦の利用条件にも戻さない。

| Issue | 実装・確認済み | 残る確認 |
| --- | --- | --- |
| #7 | 連番/ID/実行時スキーマ、欠落時snapshot復旧、期限・頻度・データ量制限、2試合分離、実localhost WebSocket＋MatchSessionの通常/音声停止2経路 | Vercel実接続での画面との通し照合。認証・利用枠・外部プロバイダーは結合試験で代替 |
| #3 | 任意の接続・解放経路 | 実APIで90秒、3回の開始/終了、実音声・映像・割り込み・遅延 |
| #8 | 実況候補の期限、優先度、重複排除、要求回数制限、直近出目の文脈、字幕保護、結果音声の接続世代分離 | 実際の発声内容と遅延、聞こえる割り込み、既にブラウザへ到達した再生音声の扱い |
| #9 | 2回の判断、公開情報のみの入力、ID/期限検証、無応答時の既定選択 | 実API判断と音声説明・リールの一致 |
| #11 | 秘密保護、認証、停止スイッチ、入力制限、120秒以内の終了開始、1試合最大2GPT接続と両世代のusage、後始末テスト | 実サービスの残存セッション/利用量、一般開放する場合の全体上限 |
| #12 | 実Chromeの通し対戦とPC描画 | 実Edge、初見の方の遊びやすさ・音質評価、実マイク拒否 |

Chromeの権限設定画面への移動はブラウザの安全規則で拒否されたため、実マイク拒否の操作は行っていない。模擬テストを実機確認とは扱わない。

`LIVE_MODE_ENABLED=false`を維持。実APIの課金利用は実施していない。接続数・日次枠はプロセス内メモリであり、再起動や別Vercelインスタンスをまたぐ全体の支出上限ではない。通常CPUデモには共有DBを追加しない。詳しくは [operations.md](operations.md)。
