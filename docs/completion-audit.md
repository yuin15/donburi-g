# 完成条件の照合（2026-09-12）

対象はユーザー指定のSlot-chan: Three.js中心の描画、無料の60秒CPUスロット対戦、2回の改造、結果と再戦。映像・音声は任意。元の音声必須仕様の課金・接続検証を通常対戦の前提へ戻さない。

公開ゲーム: `b43dc186c5e5a2ddabe2424e47a45a860c47d4f1`（PR #34）、https://slot-chan.vercel.app 。Vercel `dpl_6XdTqp9VizBKC7qxFC2u4tGsfoh7` はREADY。main CI https://github.com/yuin15/donburi-g/actions/runs/34666695337 は成功。以下はこの時点の証拠で、未確認項目を合格へ読み替えない。

## ゲーム本体

| 条件 | 判定 | 根拠・限界 |
| --- | --- | --- |
| APIキー・招待・マイクなしで通常対戦 | 確認済み | 公開ChromeでCPU入口から開始。PR #25の全試合通信観測で外部/API要求0件。`main.ts` の通常経路はローカル共通ルールを実行 |
| 60秒、双方30回転、中央1ライン、3絵柄と配当 | 確認済み | `game.ts` と `game.test.ts`。遅れたタイマーでも30回を確定。配当120/240/1200をUIも共有定義から表示 |
| 20/40秒の2択改造、同じ権利、締切・重複拒否 | 確認済み | `cpu.test.ts` とドメインテスト。両者とも操作時刻で24/44秒の締切を判定。公開Chromeで両選択を手動確定し履歴と照合 |
| 勝利・敗北・引き分け・再戦・取消 | 確認済み | `browser-release-check.md` の公開3連戦。別の固定seedテストに終盤逆転もある。固定seedを公開プレイへ注入していない |
| Three.jsの描画、確定出目と画面の一致 | 確認済み | `ReelScene.ts` / `CabinetArt.ts` と8件の描画テスト。筐体・リール・当たり・コインを1 rendererへ集約。公開結果の中央絵柄と表示値を目視 |
| 静止/非表示の負荷、後始末、低減モーション | 確認済み | `render-performance.md`、描画回帰テスト、Chromeの低減モーション設定確認。全端末60fpsや実機の電池消費は保証しない |
| 1280×720 / 1920×1080 | 確認済み | 今回も最新公開ゲームの結果画面を両サイズでDOM境界とスクリーンショット照合。3リール・得点・時間・履歴・再戦が表示範囲に収まる |
| マウス・キーボード・タッチ入力 | 確認済みの範囲あり | 実Chromeのクリック、Tab/Enter/数字キー。今回Chromeのタッチエミュレーションで開始と2回目の改造を確定。実タッチ端末やEdgeの実機結果ではない |
| タブ停止から復帰 | 確認済みの範囲あり | 終盤にChromeのページライフサイクルを25秒凍結し復帰。1秒後に残り0秒、840対1,080、安定型/大勝負の履歴、結果と再戦を確認。サーバー通信の復旧を試したものではない |
| 小当たり・7揃い・逆転・残り10秒の演出と効果音 | 実装・自動確認済み、人の評価待ち | `main.ts` の文字表示、Three.jsのコイン/発光、`GameAudio.ts` の異なる音列とミュート/解放テスト。聞き分けや音量の適切さ、遊んで気持ちよいかは初見プレイで評価する |
| 対応ブラウザ | Chrome確認済み、Edge待ち | 実Chrome 152とアプリ内Chromiumで確認。現在の接続一覧にEdgeはなく、接続案内済み。ChromeをEdgeの検証結果として数えない |
| 初見3人程度の観察、必要な調整 | 未完了 | 勝利条件、改造の意味、4秒の選択時間、音量、再戦意欲の観察結果を依頼済み。自動操作を人数へ数えない |

タッチ検証の1回目の改造はツール操作が締切に間に合わず、既定の安定型になった。2回目は受付中の大勝負をタッチで選択し、両ボタンのロックと選択済み表示を確認した。凍結・タッチ・画面サイズの検証用設定は最後に解除し、入口へ戻した。

## 秘密情報・ビルド・公開物

- 現行CIの型検査・lint・75テスト・生成後Node ESMのAPI起動検証・本番ビルド・秘密パターン検査が成功。500KB超のバンドル警告は既知。
- 追跡ファイルとローカル本番成果物の計61ファイルを、ローカルに保存した実秘密値とメール形式に対して検査。既知の秘密値を含むファイル0、メール形式を含むファイル0。値自体を出力・記録していない。あらゆる未知の秘密形式や履歴全体を保証する検査ではない。
- GitHubのsecret scanningとpush protectionは有効。非プロバイダーパターン・有効性チェックは無効。設定は閲覧のみで変更していない。
- `.gitignore` が環境変数・鍵・ローカル認証・ログを除外。現行のサーバーログ出力は利用秒数と最終確定フラグの許可形式で、会話本文や生プロバイダー応答を出力しない。実プロバイダーのログを採取したわけではない。
- `README.md`、`SECURITY.md`、`THIRD_PARTY_NOTICES.md`、`operations.md` に遊び方・開発手順・素材・制約・停止/失効手順がある。ユーザーの企画画像や人物写真を配布物として取り込んでいない。

## 任意の音声・映像

`LIVE_MODE_ENABLED=false` を維持。HTTP401 / WebSocket拒否1008はPR #34で公開確認済み。CPU通常経路はこれらのサービスへ依存しない。

| 条件 | 現状 |
| --- | --- |
| マイク拒否・初期接続失敗からCPUへ進む | クライアント回帰テストとUIのフォールバック実装あり。実ブラウザの拒否操作は未確認 |
| 試合中の音声/映像障害で同じ試合を続ける | 模擬プロバイダー/ブラウザ障害の統合テストで、同じmatchId・30スピン・2回目改造・決着と解放を確認 |
| 実音声・映像、反応、90秒生存、割込、終了、利用量 | 未確認。#3 #8 #9 の有効化前ゲート。実API利用の承認待ちで、模擬結果を代用しない |
| サーバー同期・複数クライアント・イベント欠落など #7 | 実装と部分的統合テストあり。公開Live通し・全項目の実証は未完了のまま保持 |
| 課金保護 #11 | 招待、署名、期限、Origin、停止スイッチ、プロセス内の制限と後始末を実装・模擬確認。全インスタンス共有上限や実測費用は未確認/未実装。一般の課金公開はしない |

小規模デモにUpstashは不要というユーザー指定を維持する。プロセス内カウンタを全体上限とは表現しない。任意のLiveを有効化する際に残る条件を、通常CPUゲームの完成条件から分けて明示する。

## 完成判定

通常ゲームの実装・公開・検証は上記まで進んでいるが、実Edgeと初見プレイ/音質評価が欠けるため、プロジェクト全体を完成済みとはしない。これらの確認またはフィードバックが届けば、必要な修正を行って再判定する。今回の範囲では追加の実装不具合は見つからなかった。任意Liveの未検証項目は、API利用の承認後に別途検証する。
