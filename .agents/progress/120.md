# Issue #120 進捗

- 固定 $5、方向ごとに1試合1回、借り手が $1 未満・貸し手が $5 以上・playing 中だけ成立する原子的な貸借 domain を追加。
- `loan_transfer` wire と、成立後の英語表示・DEV 両方向 fixture を追加。
- 貸与の直前に始まったリールが後から停止しても、確定済み残高へ戻さない表示補正と回帰テストを追加。
- 明示承認後、Responses に時刻順の文字起こし・残高・残り時間を限定して渡す `chooseLoanDecision`、Live delegation、貸借確定後だけの発話を追加。判定中・期限切れ・終了・音声切断では移動しない。
- ライバル側の自発提案は試合中に一度だけ。終盤の時間延長提案とは同時に出さない。
- 親側の全CIは 386 tests を通過。終盤でも時間延長の提案・判定が進行中でなければ、ライバルの借入提案を行えることを回帰テストで確認済み。

- typecheck / lint / Vitest / server runtime / build / diff check は成功。作成した例文4件の実Responses判定と、ブラウザの両方向成立表示を確認。実マイクによる通し会話は未確認。
