{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Browser client: joins the avatar's LiveKit room, streams the mic to the orchestrator, renders transcripts and tool-driven overlays.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hyperframes/player": "^0.8.16",
    "livekit-client": "^2.7.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
