{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Orchestrator: mints LiveAvatar LITE sessions and threads audio + tool calls between GPT-Live, the media server, and the browser.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ws": "^8.5.13",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
